# QISSA: controlled single-E1 dual-branch editorial acceptance

**Status as of 2026-09-18: production story-generate v99 deployed and Story AI OFF; provider-free live HTTP smoke PASS; family beta NO-GO.** Production is pinned to source commit `dd1f74c1689a186680869235abda244d9197c898` (PRs #219–#220); deployment and OFF verification are recorded in `reviews/2026-09-18_v99_production_deployment_checkpoint.md`. A 2026-09-18 re-run of the existing provider-free GitHub smoke against actual v99 passed RU/UZ/KZ fallback and both RU Space continuation fixtures; exact run/attempt, historical smoke checkout and independent unchanged claims/OFF read-back: `reviews/2026-09-18_v99_live_fallback_postflight.md`. **This was fallback, not an AI-authored literary or E2 acceptance.** v99 fixes HTTP request-attempt telemetry; v98 aligns the first-pass word target and requires causal Repair progress **in prompts and provider-free tests**, not in independently verified model output. No post-v99 paid E1 or real A/B E2 literary-quality acceptance has been established. The v96/v97 real synthetic E1 initial Narrator samples were 268/286 words, below the 320-word hard minimum; the v97 repaired E1 passed technical generation but showed repetitive waiting on editorial inspection (issue #216). Historical v94 E1 passed technically at 441 words, but its complete private envelope was not retained for E2. The earlier 2026-09-16 v81 dual-branch attempt produced fallback for both E2 calls. Historical authorizations are consumed. A future A/B acceptance requires a *new*, explicitly authorized, uninterrupted chain: one genuine E1 and at most one E2 for each actual choice. This document grants **no paid-call authorization**. Technical GREEN is not editorial PASS.

## Preconditions and immutable assertions

- Obtain fresh explicit authorization for the concrete paid scope and maximum provider work before enabling AI. Maximum *generation admissions* for a complete continuity proof: **one fresh E1 plus at most two E2 requests, one for each actual E1 choice**. If E1 is invalid or fallback, STOP after that admission. These three admissions do NOT bound internal OpenAI HTTP requests or billed tokens. As of v99, `X-QISSA-OpenAI-Request-Attempts` is a per-response count of outbound attempts, not completed/billable calls, token usage, price or invoice; the legacy `X-QISSA-Provider-Calls` header is an equivalent alias only on v99 paths. Pre-v99 values were orchestration-stage counts and must not be compared to new request counts. Daily usage columns record admissions, not HTTP attempts.
- Confirm current main SHA, exact Edge import SHA/version, JWT ON, official CI/Story Core GREEN, authoritative runtime flag OFF, deployed 180-second admission lease (`10_STORY_AI_RUNTIME_LEASE_RUNBOOK.md`), and verified test cleanup path. Historical checks must not substitute for current checks. The current `usage.ts` declares development daily limits of zero as **accounting-only**, not an enforced daily cost cap: before any external family rollout, restore an actual emergency spend/admission ceiling plus group-scoped access and verify its behavior. Never assume zero blocks all requests.
- A provider-free, fail-closed live HTTP fallback smoke **passed on v99 on 2026-09-18**; repeat it only after later functional deployments or changed runtime conditions and only with Story AI authoritatively OFF. Static deployment read-back or CI GREEN by themselves do not establish a live HTTP PASS. Do not allow a broad fallback smoke with valid consent to run while AI may be ON; it could admit multiple paid generations. No TTS or escalation by default.
- Review harness, exact scope, request ceiling and abort behavior, timeout, cleanup, credential handling and diagnostics. `scripts/story-live-evidence.mjs` allowlists NON-SECRET response headers including `X-QISSA-Generation-Failure-Class`, `X-QISSA-Generation-Failure-Trace` and request-attempt telemetry as applicable (canonical v99 header included by PR #223). Its provider-free regression is `scripts/check-story-live-evidence.mjs`. The helper itself neither performs a call nor guarantees a future runner uses it.
- Do not hardcode `choice_id='a'/'b'`: take two literal, nonempty, distinct IDs from the real E1 response and verify both resolution bridges before ANY E2. Preserve the full immutable private E1 envelope, including literary fields and choice objects, **before** starting E2. Do not publish story text, personal data, credentials or private identifiers to public issues, logs or artifacts. A reconstructed envelope is not byte-identical end-to-end proof.
- Branch A and B must clone the exact same genuine E1 literary payload, common patch and safety evidence. Change only independently assigned series/session/install identities, and then apply each *selected* choice separately. Use independent 64-hex installationAuth credentials and cleanup for both; never log them.
- Execute `applyEpisodeToSeriesState` then `applyChoiceToSeriesState`: common E1 patch before only the selected choice patch. Verify the raw saved bridge matches the actual E1 `resolution_text`; `normalizeStoryRequest` canonically substitutes the hero name with `{{HERO}}` in model-facing memory, which is expected. E2 must be segment 2, remember only its chosen ID and bridge, and expose no new choices.

## Safe lifecycle for a separately approved future attempt

1. **Provider-free preflight:** fresh CI, deployed SHA, consent/auth, explicit allowed provider work, runtime OFF and 180-second lease, branch-state fixture, immutable E1 envelope capture, response-header allowlist, and deletion armed *before* first persistence. A static mocked E2 fixture is not proof of real model performance; old fallback smokes are not genuine A/B tests.
2. **Operator lease:** trusted operator enables `enabled=true, updated_at=clock_timestamp()` only for the newly authorized work, with immediate read-back. No background renewal, no parallel families, no long sleep synchronization. An expired 180-second lease rejects *new* admissions but does not stop in-flight model work or physically reset the Boolean. Explicitly reset OFF in `finally` even on crash/fallback.
3. **One E1:** require actual `openai-structured` source, matching hero/language/episode, safety approved, no unresolved hero token, two valid and distinct choice cards with complete bridges. Capture first/final story word counts, repair flags, nonsecret response metadata, v99 attempted-request counter, and full E1 envelope privately. On failure STOP, no silent re-generation, no fallback presented as Luna.
4. **Prepare independent A/B:** arm cleanup before `sync_generated`; use 64-hex installationAuth for EVERY story-state call; apply common E1 then separately selected branch; confirm choice, reload and verify exact selected bridge/canon. Never copy A's choice history to B.
5. **Two E2 maximum, only when separately authorized and valid E1 exists:** one each for the two actual returned choices; record allowlisted headers *before* asserting source. For `generation-or-safety-failed`, record failure-class/trace or explicitly mark evidence missing. Require genuine `openai-structured` E2, approved safety, stable hero/canon, no bridge replay/phantom decision, satisfying calm payoff, and visible branch divergence. Persist/reload/compare both branches.
6. **Always OFF and cleanup:** reset the runtime flag to `enabled=false, updated_at=clock_timestamp()` in a trusted operator context; read back OFF independently. `finally` deletes each synthetic profile via authenticated `delete_profile_data`, verifies `load_current=null`, checks read-only aggregate absence of synthetic profiles/credentials and zero private transcript rows. An expired lease is not cleanup. Remove any temporary runner/workflow and verify clean diff.
7. **Independent editorial acceptance:** have a real native Uzbek age 5–7 reviewer read *both* full E1 → bridge → E2 child-visible sequences and score all 12 dimensions of `09_FAMILY_BETA_EDITORIAL_SCORECARD.md`. Each branch must score at least 20/24, with no hard failure and no required-dimension zero; collect actual parent/child read-aloud feedback and 2–4 concrete notes. Do not fabricate human assessments or claim qualification for untested language/age/world combinations.

## Stop and cost rules

- All historical E1/E2 authorizations are consumed. **No fresh E1, E2, TTS, Sol escalation or model switch is authorized by this runbook or a successful CI.** Obtain a fresh bounded approval and enforce it before paid work.
- No unbounded retries or broad sample matrix. Track generation admissions, v99 per-response outbound request attempts and actual provider invoices as distinct measurements; lack of provider usage data must be reported, never filled with guessed spend.
- Stop on SHA mismatch, broken authorization/persistence, missing E1 evidence, unexpected runtime state, safety concern or cleanup failure. Story AI OFF while reading/editing/grading stories.
- Keep links to exact runs, SHA and Edge version, nonsecret metrics, failure evidence, claim delta, independent deletion confirmation, both editorial assessments and release decision in `reviews/`. Preserve `TEMP_V81_EDITORIAL_EXECUTION_PLAN.md` until actual gates pass; remove only by reviewed PR.
