# QISSA v83 — provider-free cross-layer contradiction audit (2026-09-16)

**Status: CONFIRMED DEFECTS / STORY AI NO-GO. This is a diagnostic audit, NOT a repaired implementation, full CI result, literary score or authorization to spend.** Scope: active branch `fix/v83-e1-choice-scaffolding-repair` based on production source `f725f4fb0376b07b10fe5a8dae95bd425f32726e`; read source across Architect, Narrator, text Repair, schemas, deterministic validator and orchestration. No provider, account keys, test profiles, database writes or TTS were used in this audit.

Evidence: [read-only reproducible audit #35094936207](https://github.com/jteshaboev1984-ops/qissa_app/actions/runs/35094936207), job `104789830206`, PASS **at reproducing defects**, with nine `CONFIRMED SOURCE DEFECT` records across five classes. `PASS` means the existing bugs were demonstrated, **not** that they are fixed. The earlier separate source-branch checks [#35093721258](https://github.com/jteshaboev1984-ops/qissa_app/actions/runs/35093721258) passed prior limited checks but did not test the invariants below.

## Confirmed, independently reproduced defects

| ID | Location / contract | Reproduction and consequence | Safe correction + future regression |
|---|---|---|---|
| C1 | `repair-routing.ts` + `prompt.ts` Text Repair + `buildTextLengthRepairOutputSchema` | On standalone `story_choice_menu_scaffolding`, `invalid_title`, and `story_language_mismatch`, routing and JSON schema require a full, non-null story rewrite. The system prompt simultaneously instructs that `story_rewrite` and `story_expansion` both be null if there is no story-length/coda violation. Three reproducible contradictory cases; this was **latent**, NOT the proven cause of the historical mixed-error E1 failure. | Make the null instruction conditional on `!fullStoryRewrite && !storyTooShort`, and specify full-rewrite precedence for every routing reason. Test standalone and mixed errors, and schema/prompt agreement. Do not disable the validator or increase retries. |
| C2 | `story-architecture.ts` Architect validation vs `safety.ts` final candidate validation | An E1 blueprint with a short `tomorrow_seed`, whitespace-only `choice_icon`, or empty `value_alignment` is accepted by Architect but fails after paid narration as `invalid_tomorrow_seed`, `invalid_choice_icon`, or `invalid_value_alignment`. Three proven gaps. These fields are Architect-owned and prose repair cannot change them. | Align Architect checks with final candidate: seed >=8 characters, nonblank icon <=8 characters, nonempty allowed `value_alignment`. Add three negative fixtures proving rejection before the Narrator call. |
| C3 | `story-architecture.ts` `narrationToCandidate()` | An output with valid A and B resolution rows plus a second B row is silently accepted. `new Map()` keeps the last B text because only distinct-map size was checked. One reproduced contract ambiguity. | Require `narration.choice_resolutions.length === blueprint.choices.length`, distinct IDs and exact membership before mapping. Reject duplicates, extras and missing rows with deterministic tests. No changes to choice schema or user data. |
| C4 | `story-architecture.ts` Architect decision-point validation vs final `choiceMenuScaffoldingNeedsRewrite` | The blueprint accepts `decision_point='Buni qilish mumkin, yoki boshqacha qilish mumkin?'` even though the same menu phrasing is forbidden in child-facing E1 prose. Narrator is told both to honor this immutable decision point and to avoid choice-menu scaffolding. One reproduced upstream/downstream conflict; the actual rejected v83 E1 prose was not logged, so this is a demonstrated latent path, not a proven explanation for THAT text. | Reject Architect `decision_point` containing the same E1 choice-menu scaffolding pattern, and optionally validate overlap with structured choice labels, before paying for Narrator. Keep final child-facing validator intact and add neutral-question positive fixture. |
| C5 | `safety.ts` `validateCandidate` | When `nextEpisodePreview=null`, validator notices `invalid_preview` but then unconditionally calls `.trim()` and throws TypeError instead of returning structured error codes. One reproduced robustness bug. Strict provider JSON should normally prevent this, so this is a lower-likelihood boundary defect, not evidence of a historic provider failure. | Guard subsequent `.trim()` calls by `typeof ... === 'string'`; assert malformed inputs return `invalid_preview` without throwing. |

**Count:** C1 three variants + C2 three fields + C3 one + C4 one + C5 one = nine reproduced cases in five root classes. All were tested without contacting story-generate.

## Source-identified risks, not proven live failures

- Russian grammatical hero-token handling: final candidate validator checks `russianHeroTokenNeedsRewrite` across immutable Architect-owned preview/choice summaries/seeds, while Architect blueprint validation presently lacks the matching grammatical check. A bad immutable value could cause a futile Narrator/Repair path. Add a narrowly scoped pre-Narrator guard and RU regression; do not reject valid nominative token use. This needs its own test before being counted as a reproduced defect.
- The Uzbek `choiceMenuScaffoldingNeedsRewrite()` regex scans the final four paragraphs and may include harmless dialogue; rejected v83 raw prose was not captured, so no claim of false positive is justified. Review with synthetic positive/negative cases before changing semantics. **Never remove a safety/editorial guard solely to obtain a green run.**
- `one_time` completeness and large RU/UZ/age/world matrix are outside this v83 bedtime-series acceptance. Do not infer one-time family-beta qualification from this audit.
- Global claim counter records admitted story-generation requests, not itemized paid provider calls. An admitted request can internally call Architect, Narrator, Repair, retry and Safety. No invoice or remaining provider balance was retrieved. Existing development daily limit `0` is accounting-only, not a positive emergency production spend cap.

## Preconditions before another paid generation (in order)

1. Implement C1–C5 on a reviewed branch without weakening existing validation or adding paid retries. Consider targeted Russian immutable-value check once an offline fixture is proved.
2. Run a **new expected-fixed** cross-layer matrix: standalone/mixed repair routing and schema consistency; Architect invalid-field rejection; unique Narrator resolution IDs; menu-free Architect decision point; malformed preview returns error. Ensure current legacy tests still pass.
3. Run official full QISSA CI and Story Core proof on the exact resulting SHA, review diff and merge PR. The earlier issue creating a PR is unresolved; a branch and diagnostic GREEN alone are not merge approval.
4. Deploy exact reviewed SHA at AI OFF; verify Edge source import/JWT/180-second admission lease and read-only current counts. New one-shot live E1 only with user-approved economical scope, no retries beyond unchanged server contract, no TTS/escalation. Stop if fallback and capture safe class/trace; E2 only after real valid E1.
5. Independently assess each full E1+selected bridge+E2 A/B via native Uzbek editor and read-aloud evidence before release. Technical success is not editorial approval.

Retain this audit permanently even after the temporary execution plan is eventually deleted. Never put private balance, credentials or a real child's personal data in a public repo.
