# QISSA v85 real E1 and v86 memory integrity checkpoint

**Date:** 2026-09-16 UTC. **Status:** v85 E1 technically passed but editorially incomplete; targeted memory fix tested, merged and deployed as v86 with AI OFF. **Family-beta Story AI remains NO-GO.** This document follows [the v85 pre-choice guidance checkpoint](2026-09-16_v85_causality_deployment_checkpoint.md).

## Genuine production evidence and exact scope

- [Single v85 E1 GitHub run 35108230888](https://github.com/jteshaboev1984-ops/qissa_app/actions/runs/35108230888), job `104834877106`, completed SUCCESS. Its full HTTP response JSON and diagnostic headers are archived in the immutable job logs. No second E1 or E2 was sent in that workflow; it used one synthetic Malika profile, Uzbek Latin, age 5–7, cozy_forest, bedtime series, session 1. Two provider-free dry preflights completed first: runs `35107920277` and `35108165283`, with remote branch HEAD checking in the second.
- HTTP 200; `generation-source=openai-structured`, `story-pipeline=split-v1`, no fallback or reported failure, narrator Luna `gpt-5.6-luna`, Sol escalation false, model safety `approved=true`, risk low, action publish. Source and safety are technical acceptance, not literary or independent child-safety certification.
- Title `Chirqilloqning mayin qo‘shig‘i`, E1 `story_text` 374 words; both unique choice IDs present. A is a turn-taking song game (`navbat_bilan_qoshik`), B an echo/call-and-response game (`aks_sado_oyin`). Their bridges contain 29 and 28 words respectively: within the 25–60-word hard gate, below the 30–45-word editorial target.
- Diagnostics: initial story 246 words, final 374; `generation-repair=text-length`, `repair-retry-used=true`, `provider-calls=6` (logical first-party diagnostic, **not** an invoice); no Sol escalation. Only final E1 JSON and metrics were captured. Original Architect blueprint, pre-repair Narrator prose and exact contents of rejected repairs were **not** logged. Do not attribute the short output or repeated paragraphs conclusively to a particular stage.
- Compared with historical [v84 E1](2026-09-16_v84_e1_live_quality_checkpoint.md), the initial story no longer shows the full selected game's success before choice: Chirqilloq is not yet heard by the group, and Malika must initiate the selected game for the first clear payoff. The two bridges encode visibly different games and relationship facts. This is an internal editorial observation on this E1, **not** proof the E2 branches diverge or that prompt guidance generalizes.
- Remaining visible prose issues: multiple late paragraphs repeat readiness, watching and waiting; some Uzbek phrases feel abstract or unnatural for a young child (e.g. `hammamizga qulay bo‘ladigan o‘yin`, `xotirjam bo‘ladigan o‘yin`, `maydon ustida yoqimli havo aylanardi`). No independent native Uzbek editor or parent/child read-aloud has reviewed this sample. The selected bridges already give the sparrow an immediate successful audible result; Episode 2 must still offer a substantial new, non-repetitive continuation without inventing another problem. **Both real E2 branches are UNTESTED.** Do not assign the 24-point whole-session score from E1 alone.

## Three actual broken memory events and engineering root

The returned top-level `state_patch.last_event` ends after `... yangi o‘yin`, branch A after `... ovozi do‘stlariga`, and branch B after `... o‘yinini boshlab,`. These are not complete events and must not be passed downstream as valid canon. A source inspection isolated a concrete deterministic contributor: `finalPatchFromCandidate` called `compactMemoryText(..., 96)` for each last_event; that function cuts at a word boundary within 96 characters, which does not preserve a sentence. The original raw Architect event texts are unavailable, so this proves a truncation mechanism and reproduced outcome, **not** that raw events were necessarily complete.

- [PR #197](https://github.com/jteshaboev1984-ops/qissa_app/pull/197) changes **only** last_event serialization: preserve complete events through 300 characters (aligned with the incoming E2 summary budget); above budget, retain the last completed sentence that fits, or omit the event if none fits instead of storing a fragment. It leaves other compact memory caps, models, provider retry, safety, schema, RLS and TTS unchanged. It cannot retroactively restore the clipped v85 JSON; it protects subsequent output when the raw event is complete.
- Added `scripts/check-story-memory-event-integrity.mjs` to official `check:story-ai-split`. The offline regression constructs **synthetic completed continuations** of all three real over-96-character patterns (does not claim to reconstruct the original raw model result), tests E1 top/A/B through `buildFinalEpisode`, a `normalizeStoryRequest` E2 roundtrip, complete-sentence extraction above 300, omission without sentence boundary, and legacy concise memory. Zero model/HTTP/database calls.
- [QISSA CI #443](https://github.com/jteshaboev1984-ops/qissa_app/actions/runs/35109269877) intentionally failed on the precise old 96-character truncation. A single isolated temporary patch workflow applied the one-file change, passed the offline checks, and was deleted with its trigger before merge. Final diff: `contracts.ts`, offline regression and one `package.json` test-wiring line only. [QISSA CI #448](https://github.com/jteshaboev1984-ops/qissa_app/actions/runs/35109589229) SUCCESS and [Story Core #271](https://github.com/jteshaboev1984-ops/qissa_app/actions/runs/35109589219) SUCCESS. PR #197 merged as `fe4c02e13356b9103dc1b97e26801e7cad61f1d7`.
- Production `story-generate` independently read back **v86 ACTIVE**, `verify_jwt=true`, entrypoint pinned exactly to merge SHA `fe4c02e13356b9103dc1b97e26801e7cad61f1d7`. Read-only SQL immediately before and after deployment confirmed `qissa_runtime_flags.story_ai_enabled=false` and today's admission count `qissa_provider_daily_usage.story_generation_claims=17`. One v85 E1 changed the count 16→17; there was **no additional paid generation** for the fix or deployment. No production schema, secrets, TTS settings or actual user data were changed.

## Stop line and next controlled acceptance

1. Keep production Story AI OFF; archive this v85 E1 and its malformed last_event as diagnostic evidence. Do not feed truncated output into real E2, claim v86 retroactively repaired the old response, or rerun the historical paid workflow. Do not run an unbounded paid test matrix.
2. The next AI expense, if deliberately authorized, is a fresh **one-shot v86 E1** after an exact-source/JWT/runtime/counter preflight. Review causal choice, complete top/A/B memory, branch bridge length, natural spoken Uzbek, repetition and adequate plot runway for the same-night E2. Stop on fallback, malformed memory or decorative choice. Favor inspecting final logged evidence before another paid attempt.
3. Only after the new E1 passes internal editorial inspection, run one strictly isolated E2 for A and another for B using identical original E1 with only the selected bridge/choice and their own branch state; verify post-bridge continuation, distinct interactions, closed bedtime ending and persistence/reload without mixing branches.
4. Family-beta qualification requires both full real sessions, at least 20/24 with no hard fail under [the scorecard](../09_FAMILY_BETA_EDITORIAL_SCORECARD.md), and independent native Uzbek and parent/child read-aloud review. Until that evidence exists: **NO-GO**.
