name: Temporary Story AI Rollout Gate Patcher

on:
  push:
    branches:
      - fix-story-ai-explicit-opt-in-20260915

permissions:
  contents: write

jobs:
  patch:
    if: contains(github.event.head_commit.message, 'Run Story AI production rollout gate patcher')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: fix-story-ai-explicit-opt-in-20260915
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - name: Apply production rollout gate
        run: python3 scripts/temp-story-ai-rollout-gate.py
      - name: Remove patcher script
        run: rm scripts/temp-story-ai-rollout-gate.py
      - name: Validate focused contracts
        run: |
          npm ci
          npm run check:story-cost-guard
          npm run check:story-ai
          npm run check:story-ai-split
          npm run typecheck:story-ai
          npm run typecheck
          npm run build
      - name: Commit durable changes
        run: |
          git config user.name qissa-audit-bot
          git config user.email actions@users.noreply.github.com
          git add -A ':!.github/workflows/temp-story-ai-rollout-gate.yml'
          git commit -m "Keep Story AI behind production rollout gate"
          git push origin HEAD:fix-story-ai-explicit-opt-in-20260915
