name: TEMP debug
on:
  push:
    branches: ["fix/v87-choice-menu-diagnostics"]
    paths: ["audit/TEMP-debug.txt"]
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
