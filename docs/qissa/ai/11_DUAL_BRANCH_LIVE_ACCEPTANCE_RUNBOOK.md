# QISSA: controlled single-E1 dual-branch editorial acceptance

**Status: FIRST LIVE ATTEMPT FAILED, APPROVED PAID SCOPE EXHAUSTED.** Original 2026-09-16 attempt: genuine E1 succeeded, both independent E2 calls returned `safe-fallback`, and the audit runner omitted failure-class/trace headers. Permanent evidence: `docs/qissa/ai/reviews/2026-09-16_v81_single_e1_dual_e2_live_attempt.md`. This is a protocol for a POSSIBLE FUTURE attempt, **not permission to execute one**. Keep Story AI OFF. A technical GREEN is not a family-beta editorial PASS.

## Preconditions and immutable assertions

- Obtain a NEW explicit authorization for concrete paid scope and ceiling before enabling any AI. The previous three generation claims (one E1 plus one E2 per branch) are fully consumed. Internal Architect/Narrator/Repair/Safety/Moderation calls can multiply provider requests, and `X-QISSA-Provider-Calls` is neither a full HTTP tally nor an invoice. Never enable TTS or escalation by default.
- Confirm exact current `main`, current Edge `story-generate` import SHA and `verify_jwt=true`, official CI/Story Core GREEN, authoritative runtime flag OFF, and the deployed 180-second server-side admission lease (`docs/qissa/ai/10_STORY_AI_RUNTIME_LEASE_RUNBOOK.md`). Current code/flag state must be rechecked; historical version numbers are evidence, not automatic current state.
- Pre-review the audit harness, exact scope, call count, timeout, cleanup, credentials handling and diagnostic evidence. `scripts/story-live-evidence.mjs` provides an allowlist for NON-SECRET response headers including `X-QISSA-Generation-Failure-Class` and `X-QISSA-Generation-Failure-Trace`, and `selectActualStoryChoices` for dynamic real IDs. Its offline regression is `scripts/check-story-live-evidence.mjs`. The helper does not trigger any provider call. A future runner must actually use the helper; existence alone does not prove that live diagnostics are captured.
- Do not hardcode `choice_id='a'/'b'`. Architect controls IDs; select by the two returned choice objects and persist their original literal IDs. Reject missing/duplicate IDs and incomplete bridges BEFORE an E2 provider request. Validate the entire returned E1 JSON envelope; preserve a complete immutable private evidence snapshot (with identifiers/credentials redacted where necessary) **before** starting E2. Do not publish children's personal data or secrets to public logs or artifacts. If recovery requires reconstructing a missing envelope, explicitly label continuity evidence as limited, never byte-identical end-to-end.
- Both branches must be clones of exactly ONE genuine E1's literary fields, choice objects, top-level patch and safety evidence; remap only per-branch series/session identity. Two random UUID installations and distinct series/session IDs, independent 64-hex `installationAuth` credentials and separate cleanup. Never echo credentials or publishable keys in logs.
- Use actual `applyEpisodeToSeriesState` THEN `applyChoiceToSeriesState` for each branch. Apply E1's common patch before only the selected choice patch. Verify raw client-state `resolution_text` remains literal; `normalizeStoryRequest` intentionally substitutes the canonical hero name with `{{HERO}}` in model-facing memory. Both forms are expected and must NOT be mistaken for different stories. Confirm E2 segment=2, remembered selected choice ID/bridge, no unselected history and zero new choice fields.

## Safe lifecycle for a separately approved future attempt

1. **Provider-free preflight:** check official CI, deployment exact SHA, consent/auth, allowed request count, runtime OFF and short lease, independent memory fixture, E1 full-envelope capture, all non-secret diagnostic headers, and cleanup arming before first persistence call. Do not reuse old 12-scenario fallback smoke as a genuine A/B provider test. Check `scripts/check-story-live-evidence.mjs` and its CI result. Distinguish static mock E2 blueprint acceptance from real model performance: a green fixture cannot explain the 2026-09-16 failures.
2. **Short operator permission:** trusted operator sets `enabled=true, updated_at=clock_timestamp()` only immediately before approved requests. No automatic lease renewal. Each new window requires verifying accounting and scope; lease expires for NEW calls in 180 seconds but does not cancel admitted in-flight calls or physically turn the stored Boolean OFF. Operator must set OFF explicitly after admission, even if job hangs.
3. **One E1:** require `openai-structured`, correct episode/hero/language, safety approved, no unresolved token, two actual distinct choices with both bridges. Record full non-secret response metadata, first/final words and repair flags, and controlled complete E1 envelope. On invalid E1/fallback STOP; do not silently regenerate or substitute a fallback as Luna text.
4. **Prepare A/B:** arm cleanup BEFORE first `sync_generated`, use 64-hex `installationAuth` for EVERY `story-state` call, independent identities, common E1 application then per-branch selected choice, `confirm_choice`, reload and verify the exact selected bridge and compact canon. No copy of A history into B.
5. **Two E2 maximum IF freshly authorized:** each exactly once with independent selected state. Capture all allowlisted headers and log safe diagnostic metadata BEFORE asserting success/fallback. If response is `generation-or-safety-failed`, require failure-class and failure-trace: if missing, explicitly mark DIAGNOSTIC EVIDENCE MISSING without pretending to know the cause. Other fallback reasons remain separately classified. A/B must return genuine `openai-structured` E2, safety approved, hero/canon preserved, no replay of bridge/phantom choice, complete calm local payoff and visibly different downstream scenes. Persist/reload and compare each.
6. **Always cleanup and OFF:** explicit trusted DB `enabled=false, updated_at=clock_timestamp()` and read-back independently of runner; `finally` removes EACH synthetic profile via authenticated `delete_profile_data`, `load_current=null`, confirm read-only DB absence of profiles/credentials. If runner crashes, lease only prevents new admissions after expiry; explicitly turn OFF and inspect residual data. Delete temporary runner/workflow and verify zero diff before finalizing.
7. **Independent editorial acceptance:** assess both complete child-visible E1→selected bridge→E2 sequences with all 12 rubric dimensions in `09_FAMILY_BETA_EDITORIAL_SCORECARD.md`. EACH >=20/24, no hard failure or required-dimension zero. Require independent native Uzbek editor and parent/child read-aloud notes (do not fabricate), 2–4 concrete notes, clear PASS/ITERATE/REJECT. Qualify only tested language/age/world combinations.

## Stop and cost rules

- Prior 1 E1+2 E2 permission is exhausted. **No further paid attempts without renewed explicit scope and ceiling.** Do not grant runtime permission merely because the runbook, helper or CI exists.
- No unbounded retry, TTS, escalation, broad matrix or blind rerun on fallback. Track actual generation claims separately from internal debug counters and actual invoice.
- Stop on SHA mismatch, broken authorization/persistence, missing E1 evidence, unexpected runtime state, safety concern, or cleanup failure. Runtime must be OFF when reading/editing/grading stories.
- Save links to exact runs, SHA/Edge version, nonsecret metadata, all failure-class/trace evidence, provider claims, independently verified cleanup, both literary assessments and release verdict in permanent `docs/qissa/ai/reviews/`. Preserve `TEMP_V81_EDITORIAL_EXECUTION_PLAN.md` until all release gates truly pass; then delete only through a reviewed cleanup PR.
