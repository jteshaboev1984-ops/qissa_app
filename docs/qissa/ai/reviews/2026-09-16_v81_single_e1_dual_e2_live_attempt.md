# v81 controlled provider acceptance: one genuine E1, attempted A/B E2 — NOT QUALIFIED

**Date:** 2026-09-16 UTC. **Release status: ITERATE / NO-GO for AI family beta.** This is the actual result of the user's authorization for precisely one fresh E1 and two E2 requests, not a hypothetical plan or independent literary review. Do not interpret a fallback as a Luna-written E2.

## Version and authorized scope

- Repository main/deployed exact SHA: `1f59ef09128122ec0ccd430566a8a516be925fbf`, editorial PR #186 merged; Edge Function `story-generate` v82 ACTIVE, JWT verification enabled; underlying 180-second lease from PR #187.
- UZ / age 5–7 / `cozy_forest` / `bedtime` / `series`, synthetic custom hero Malika; no TTS, no escalation. Three provider-eligible story-generate requests maximum: 1 E1 + 1 E2(choice A) + 1 E2(choice B); no paid reruns.
- Official editorial head before merge: [CI #388](https://github.com/jteshaboev1984-ops/qissa_app/actions/runs/35080950252) GREEN, [Story Core #224](https://github.com/jteshaboev1984-ops/qissa_app/actions/runs/35080950253) GREEN. These confirm software checks, not actual literary quality.

## Actual timeline and evidence

1. Provider claim baseline **9** on 2026-09-16 UTC. Runtime was OFF. An intentionally disarmed temporary workflow [run #35082909419](https://github.com/jteshaboev1984-ops/qissa_app/actions/runs/35082909419) was SKIPPED.
2. First live [run #35083106909](https://github.com/jteshaboev1984-ops/qissa_app/actions/runs/35083106909), job `104751515156`: exactly one E1 produced `openai-structured`, Luna, safety approved, no escalation; initial 260 → final **357 words**, `text-length` repair, diagnostic `X-QISSA-Provider-Calls=5`. Two real choice IDs were `choice_song_circle` and `choice_song_echo`. **The audit runner failed on its own erroneous assumption that IDs must literally be `a` and `b`**, before state persistence or any E2 request. It is a harness defect, not an E1 model-validation failure. Operator returned runtime OFF at 10:07:07 UTC. Daily claims increased to **10**.
3. A provider-FREE recovery [run #35083547583](https://github.com/jteshaboev1984-ops/qissa_app/actions/runs/35083547583), job `104752949788`, read previous authenticated GitHub job logs and verified exact E1 `title`, `story_text`, both choice objects and E1 `state_patch`. It did not call story-generate. Because the first job logged only these data rather than the full provider response, the later persistence envelope was reconstructed using the verified E1 ID and a valid per-branch series ID, `generationSource`, and minimal `safety_self_check.approved=true` from the first runner's already-passed assertion. **Do not claim byte-identical full raw E1 JSON or a single uninterrupted UI session.** Child-visible literary text and choice objects remained unchanged.
4. Continuation [run #35083675997](https://github.com/jteshaboev1984-ops/qissa_app/actions/runs/35083675997), job `104753366534`: separate cryptographically generated 64-hex installation credentials, separate installation/session/series identifiers and exact pure product `applyEpisodeToSeriesState` → `applyChoiceToSeriesState`. Both `sync_generated` E1 and `confirm_choice`/`load_current` succeeded, with real choice IDs and saved `resolution_text` matching their respective choices. Two E2 generation requests were issued **once each** in parallel. They both returned source **`safe-fallback`** and reason **`generation-or-safety-failed`**. Branch B header: diagnostic provider calls 2, no `narrator-model-used` value, repair `none`. Branch A header: diagnostic provider calls 4, narrator Luna, repair `text-length`. No authentic E2 text was returned or captured, and therefore no quality, continuation, phantom-choice, or branch-divergence PASS can be claimed. Job concluded **FAIL**; no retry.
5. Claims reached **12**, exactly +3 total compared with baseline. This number is provider-eligible generation *claims*, **not** the number of provider HTTP calls or invoiced spend. Combined debug counters (5 + 2 + 4) are diagnostic and do not equal a bill. Operator set `story_ai_enabled=false` at **2026-09-16 10:13:39.362516 UTC** after both E2 admissions, while completions were in flight. Confirmed OFF again at 10:14:14 UTC.
6. Runner logged `CLEANUP PASS` independently for both synthetic installations, including `delete_profile_data` and `load_current=null`; approved read-only production SQL independently returned **0 `child_profiles` and 0 `installation_credentials`** for installation IDs `2456e950-c781-4bea-8913-84eac9470c18` and `ed8b115e-793d-4ae3-8e7b-c13cdee591b6`. First failed E1 test did not create a profile. All temporary audit runner/workflow files were deleted from `audit/v81-single-e1-dual-e2-once`: compare `main...a59350c1917396ab991c04cb4561ac6622784985` yielded **zero changed files**. No paid trigger remains in this branch.

## Authentic E1 literary evidence

Title: **Tikan uchun quvnoq qo‘shiq**. Hero: Malika. Final words: 357. The E1 story and full structured choice objects are preserved verbatim in [job 104751515156](https://github.com/jteshaboev1984-ops/qissa_app/actions/runs/35083106909). The following two child-visible options and immediate bridges are extracted from that real run, not rewritten:

- A, `choice_song_circle`: `Do‘stlarni yoniga chaqirib, navbat bilan ovoz qo‘shishni boshlash`. Bridge: `Malika do‘stlarni yaqiniga chaqirdi. Qanotcha, Baqa va Shirin navbat bilan ovoz qo‘shdi. Tikan kulib yubordi, keyin qo‘shiqka o‘zi ham qo‘shildi. Eman tagida hamma birga xursand bo‘ldi.`
- B, `choice_song_echo`: `Eman yonida turib, quvnoq ovoz aytib, do‘stlardan aks-sado kabi javob so‘rash`. Bridge: `Malika quvnoq ovoz aytdi. Qanotcha, Baqa va Shirin unga navbat bilan javob berdi. Tikan ularning kulgili aks-sadosini eshitib, dumaloq aylandi va quvonch bilan qo‘shiq kuyladi.`

**Internal qualitative E1-only observations, not a 24-point session score:** There is more singing, concrete animal noises and a hedgehog with a moving nose; however Malika largely observes the other animals until the choice, several paragraphs describe waiting/smiling, and both options revolve around versions of the same song. Uzbek needs native editorial review (e.g. `Tikaning`, `qo‘shiqka` forms). E1 is below the soft target 380–420 but above the hard lower bound 320. With both real E2 missing, the rubric requires **no final score or editorial PASS**.

## Failure diagnosis: what is established versus unknown

Established: two E2 admissions, source `safe-fallback`, common coarse reason `generation-or-safety-failed`; asymmetric debug traces (B 2 calls/no repair, A 4 calls/length repair). Both independently persisted selected E1 choice successfully *before* continuation requests. This is not a permission-lease expiration finding; each response had `x-qissa-runtime-ai=enabled` and admission was within three minutes.

**Unknown:** exact `X-QISSA-Generation-Failure-Class`, `X-QISSA-Generation-Failure-Trace`, blueprint validation errors or rejected text. They are generated by production code but the temporary harness logged only a short header allowlist omitting them. Supabase connector currently offers no production Edge runtime-log retrieval action. Do **not** attribute failure specifically to Architect, Narrator, safety, lease, or the editorial prompt from these logs alone. Do not regenerate paid stories on speculation.

## Next authorized work (provider-free only until new explicit authorization)

1. Record and fix test harness assumptions about dynamically generated `choice_id`s; always use the actual `choices[0/1].choice_id`, never a hardcoded `a/b` contract. Reconstructing logged E1 was a recovery accommodation, not a replacement for complete raw JSON evidence.
2. Make the *next* audit runner log failure class/trace and all relevant diagnostic headers on fallback, redact credentials and keys, and retain immutable raw E1 evidence securely before starting; distinguish content validation from operational harness errors. Review potential production regressions **offline** against known schema, validators and repair behavior first; no additional paid calls under exhausted scope.
3. Require full same-E1 E2 for both A/B and independent native Uzbek editorial + family read-aloud approval using `09_FAMILY_BETA_EDITORIAL_SCORECARD.md` (each >=20/24 with no hard fails), on a separately approved later test only if justified.
4. Keep Story AI OFF, no TTS/escalation, retain `TEMP_V81_EDITORIAL_EXECUTION_PLAN.md` until qualification; mark this test ITERATE/NO-GO rather than treating fallback as Luna output.
